version: 3

project:
  locator: solaceservices_sempv2-interface
  id: solaceservices_sempv2-interface
  name: sempv2-interface
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
